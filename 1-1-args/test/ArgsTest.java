package com.clo.tdd.args;

import org.junit.Test;

import static org.hamcrest.core.Is.is;
import static org.junit.Assert.assertThat;

/**
 * com.clo.tdd.args.ArgsTest
 *
 * @author Cloneable
 * @date 2019/8/22 20:48:22
 * @description
 */
public class ArgsTest {
    @Test
    public void should_get_args_list_when_give_args_schema_and_args_string() {
        //String schemaStr = "l:boolean;p:int;d:string";
        //Schema schema = new Schema(schemaStr);
        //
        //String argsStr = "-l -p 8080 -d /usr/logs";
        //ArgList args = new ArgList(schema, argsStr);
        //
        //Object argVal = args.get("l");
        //assertThat((Boolean)argVal, is(true));
    }
}
